# PdfZero — local environment variables
# Copy this file to .env.local (gitignored) and fill in real values.

# GLM coding-plan API key for the in-editor EN↔ES text translation.
# Create it at https://z.ai/manage-apikey/apikey-list (GLM Coding Plan section).
# Used by src/lib/translation.js — never commit real keys.
VITE_GLM_API_KEY=
